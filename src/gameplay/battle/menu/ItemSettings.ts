import { Padding } from "./Padding";

export interface ItemSettings {
    width: number;
    textColor: number;
    fontSize: string;
    padding: Padding;
    bgColor?: number;
    selectedBorderColor: number
}
