import Phaser from 'phaser'

const DiscoveryAction = (layerObject, world) => {
  // const rect = new Phaser.GameObjects.Rectangle(world, layerObject.x, layerObject.y, layerObject.width, layerObject.height, 0xff0000, 0.5)
  // world.physcs.existing(rect)
}

export default DiscoveryAction
